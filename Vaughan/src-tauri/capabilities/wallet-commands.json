{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "wallet-commands",
  "description": "Capability for wallet commands - accessible from main window only",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
