{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dapp",
  "description": "Capability for dApp windows - minimal permissions for security",
  "windows": ["dapp-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-title"
  ]
}
